apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: izleme
spec:
  ingressClassName: nginx
  rules:
    - host: grafana.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: izleme-grafana
                port:
                  number: 80
