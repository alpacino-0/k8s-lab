kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080   # cluster icindeki NodePort
        hostPort: 8080         # Mac'inin portu
        protocol: TCP
  - role: worker
  - role: worker
