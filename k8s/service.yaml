apiVersion: v1
kind: Service
metadata:
  name: k8s-lab-app
spec:
  type: ClusterIP          # artik disariya Ingress uzerinden aciliyor
  selector:
    app: k8s-lab-app
  ports:
    - port: 80
      targetPort: 3000
