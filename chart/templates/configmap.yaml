apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  {{- range $anahtar, $deger := .Values.config }}
  {{ $anahtar }}: {{ $deger | quote }}
  {{- end }}
